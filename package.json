{
  "name": "personal-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Model-agnostic, secure, mobile-first personal AI orchestrator",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start:all": "bash scripts/start-all.sh",
    "verify:ready": "bash scripts/verify-ready.sh",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
